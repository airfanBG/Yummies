@model List<CategoryViewModel>

@for (var i = 0; i < Model.Count; i++)
{

    <div class="col-md-4">
        <input type="checkbox" />
        <label asp-for="@Model[i].CategoryName">@Model[i].CategoryName</label>

    </div>

}